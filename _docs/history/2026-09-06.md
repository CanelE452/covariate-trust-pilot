# 2026-09-06 — PROB-HEAD-STRUCTURE-FULL-v1 동결 v4 게이트 재실행

## 무엇을 고쳤나 (계약 위반 → 수정)

| 위반 | 수정 |
|---|---|
| `stages.py` 가 s1/s2/s3/r1/r2/r3/a1/a2/b1/b2 PASS 를 자체 임계값 비교로 산출 | 모든 판정을 `gates.py` reducer 로 이관. 신규 `gate_records.py` 가 GateResult 전체를 봉인·복원 |
| real 게이트를 M5 단독으로 계산 | 5개 우선순위 데이터셋 전부 감사. Auto/Carparts/RAF 는 geometry-only, OnlineRetail 통과 → 매니페스트 `(m5, online_retail)` |
| `stage_s2_analysis` 가 `DGP_BALANCE=PASS` 를 하드코딩 | 원장에서 실제 상태를 읽음. confirmatory 사다리가 실패 lineage 를 거부하므로 동일 산술을 진단으로 표기 |
| 최종 권고를 `S1 PASS` 로 산출 | 동결 truth table 대로 `characterization_clear = S3 GO 또는 R2 GO` 만 입력 |
| S3 를 3개 주효과·CI 없이 계산 | 동결 추정기의 3쌍 × 17대조 = 51개 요구를 그대로 사용 |
| Stage B 가 라우터 가중치를 위치로 정렬 | `dataset_id/series_id/origin` 명시 키 조인. 위치 concat 금지 |
| Stage B 캐시에 내용 결속 없음 | `_cache_v2` 가 사전등록·매니페스트·체크포인트·모듈 해시에 결속 |

## 변경 파일

- `experiments/prob_head_structure_full_v1/stages.py` — 30개 스테이지 전부 재작성(합성/real/풀/A/B/C/통제/부트스트랩/최종)
- `experiments/prob_head_structure_full_v1/gate_records.py` (신규) — GateResult 직렬화·복원·수집
- `experiments/prob_head_structure_full_v1/resource_guard.py` (신규) — 프로세스 RSS 감시, 예산 14.3 GB
- `experiments/prob_head_structure_full_v1/run.py` — 메모리 감시 연결, `MEMORY_CAP_PARTIAL_COMPLETION`, CDF 풀은 resume
- `tests/prob_head_structure_full_v1/test_frozen_gate_execution.py` (신규) — T25~T43 회귀 22건

## 실행 결과 (30단계 완주, wall 0.196 h)

동결된 20만 카운트 exact-CDF 가드가 OnlineRetail 에서 NB·HSNB 두 head 를 종단 차단.
세 계열 비교가 모든 선택 데이터셋에서 성립하지 않아 R1/R2/R3, A1~A4, B1/B2, C1/C3,
통제 3종이 NOT_EVALUATED(과학적 FAIL 아님).

```
확정 FAIL   DGP_BALANCE(3셀 불균형) · S2(cell oracle gain 0.015% < 2%) · C2(AUPRC 0.55 < 0.70)
확정 PASS   S1 (진단 — DGP_BALANCE 실패로 confirmatory 자격 없음)
NOT_EVAL    S3 · R1 · R2 · R3 · A1~A4 · B1 · B2 · C1 · C3 · CONTROL_A/B/C
최종        ALL_NEW_METHOD_BRANCHES_NO_GO
```

진단 관측: P2 풀이 HSNB 단독으로 붕괴 → 외부에서 최선 단일 대비 29.8% 악화.
증류 A0(7.267)가 A1/A2/A3 전부보다 우수. B 확장 특징이 baseline 보다 근소 열세
(Spearman 0.208 vs 0.214). C2 센서가 기준 특징 대비 AUPRC +0.008 (요구 +0.05).

## 보존

기존 보고서 3종 + 낡은 판정을 담은 attempt 93개를 `results/.../report_history/` 와
`runs/.../_superseded/` 로 바이트 보존 이동(삭제 없음).

커밋 `c613590`, `origin/prob-head-structure-full-v1` 푸시 완료.
