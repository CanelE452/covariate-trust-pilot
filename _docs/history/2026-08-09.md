# 2026-08-09

## 논문 관점 전면 재정리 — 실험 0회, artifact 감사 기반

새 실험·학습·TEST 채점 0회. commit/push 하지 않았다 (프로토콜 17번).
산출물 `results/paper_synthesis/` 20개.

### 감사 범위와 가장 중요한 발견

`results/` 전 영역에서 56개 항목을 읽어 `artifact_audit.json` 으로 정리했다
(영역별: numerical 1, StageA 8, posthoc 6, regime 4, H2 independent 4, priority 5, routing 28).

**합성 study(Stage 1/3/4) artifact 가 이 저장소에 없다.**
`_docs/history/2026-08-07-migration.md` 에 따르면 원본은 `~/Documents/github/m5dataset`,
**git remote 0개**, 마지막 커밋 2026-06-25, 그리고 현재 작업 머신과 다른 호스트다.
이관된 것은 external-validity 이후뿐이다. 따라서 C1(controlled characterization)의
숫자는 지금 artifact 로 인용도 검증도 불가능하다. 이 한 건이 판정을
`PAPER_READY_WITH_CURRENT_EVIDENCE` 가 아니라
`PAPER_READY_AFTER_MINIMAL_EXPERIMENTS` 로 만든 유일한 이유다.

참고: 이 저장소 이름·README·`results/report.md`·`results/figures/` 는
Chronos-2 covariate pilot 것이고 이 논문과 무관하다.

### artifact 에서 다시 읽은 핵심 수치

```
H1 spearman            M5 +0.1065 [+0.0437,+0.1652]   Favorita +0.0789 [+0.0205,+0.1405]
  3 scale x 2 dataset  6/6 양수, 6/6 CI 0 제외
  adjusted 표준화계수  +0.032 / +0.017  (둘 다 CI 0 포함)
  SBC intermittent     relative +0.153 [+0.052,+0.261]  scaled +0.123 [+0.013,+0.227]
  SBC lumpy            +0.014 / +0.032 / +0.028  (전부 CI 0 포함)
H2 독립 M5 (675/5018)  effect -0.0230 [-0.0294,-0.0163]   point win +11.87pp [+7.85,+15.81]
  seed 0/1/2           -0.0230 / -0.0211 / -0.0277  (3/3 CI 0 제외)
  overlap 보정          +0.0032 [-0.0033,+0.0094]  (CI 0 포함)
  matching             worst SMD 0.614 on log_train_scale -> 실패
H3                     M5 -0.0305 / Favorita -0.0428  (둘 다 예측과 반대 부호, CI 0 포함)
occurrence gate BSS    M5 -0.0084 [-0.0411,+0.0241]   Favorita -0.0908 [-0.1401,-0.0438]*
classical mean rank    M5: SBA 3.152 < Croston 3.260 < TSB 3.411 < SES 3.483
                           < dlinear_point 4.202 < dlinear_hurdle 4.220
convex oracle          M5 best static 1.0087 -> convex 0.9673 (gain 4.11%)
expert diversity       geometric ceiling multiplier 2.15, 실현 이득은 +0.43%
external benchmark     Fresh -2.43% [-2.74,-2.13]*  UCI +0.13% [-0.06,+1.13]
sequence gate          Fresh +2.648% [+2.068,+3.287]*  UCI -193.9%
```

### 판정

- 추천 scope: **B** (controlled characterization + real-data boundary + adaptive-use boundary)
- Contribution 3개: C1 통제 특성화 / C2 실데이터 검증과 얽힘 경계 / C3 적응적 활용 경계
- 가설: H1 `SUPPORTED_WITH_BOUNDARY`, H2 selector `CONFIRMED` + mechanism `NOT_REPLICATED`,
  H3 `NOT_REPLICATED`, routing method `NOT_REPLICATED` / boundary `CONFIRMED`
- MAIN 11개 / APPENDIX 15개 / 나머지는 개발 history 로 강등
- MUST_HAVE 1건: 합성 study artifact 회수·검증 (실험 아님, 사용자 승인 대상)
- routing 후보는 전부 DO_NOT_RUN — `ROUTING_MODEL_DEVELOPMENT_STOP` 유지

### 새로 확정한 서술 경계

- occurrence gate 메커니즘은 **실데이터에서 기각**됐다 (상수 baseline 대비 skill 없음).
  reader chain 5번을 "통제 study 안에서만" 으로 다시 썼다.
- expert diversity 는 **ceiling 을 올렸지 성능을 올리지 않았다** (2.15배 vs +0.43%).
  reader chain 8번에 이 문장을 추가했다.
- 결론 문장을 반증 가능한 형태로 교체했다 —
  "측정 가능한 oracle 기회가 학습 가능한 routing 함수의 증거는 아니다".

상태: `PAPER_SYNTHESIS_READY_FOR_REVIEW`

---

## 합성 study source 회수 시도 — 이 머신에 없음, transfer 필요

실험·학습·TEST 채점 0회. `results/paper_synthesis/` 미수정. commit/push 없음.
새 파일 1개: `results/synthetic_source_recovery/synthetic_transfer_manifest.md`.

### 0단계 — 경로를 가정하지 않고 실제 검색

```
find /c/Users /d /e /f /g -maxdepth 6 -iname "*m5dataset*"   -> repository 0건
  (히스토리 사본 3개 + Claude 세션 디렉토리 1개뿐)
*.bundle / *m5*.tar* / *m5*.zip 검색                          -> 0건
experiments/temporal_dependence/                              -> 없음
experiments/decomposition_when_helps/                         -> 없음
```

다른 세션이 2026-08-08 에 **독립적으로 같은 검색을 하고 같은 결론**에 도달한 기록이
`_docs/history/2026-08-08.md` 에 있다 (0건, `unified_temporal_27_v3` 는 코드만).

-> `SYNTHETIC_SOURCE_TRANSFER_REQUIRED`. 1단계 규칙대로 여기서 중단했다.

### 다만 새로 확인한 것 — 방법 코드 일부는 로컬에 있다

```
experiments/unified_temporal_27_v3/scenarios.py   27 시나리오 (A12+B9+C6).
                                                  Group A 가 marginal 보존 통제다 —
                                                  iid 변형이 구조화 pool 의
                                                  split-local 무작위 재배열.
experiments/unified_temporal_27_v3/config.py      length 512, 358/409, rho 0.30/0.70
experiments/om_factorization_killtest/prereg.py   PRIMARY_METRIC "rmse_mean_truth"
experiments/om_factorization_killtest/models.py   PointDLinearParamMatched /
                                                  FactorizedDLinear
```

**그러나 둘 다 paper DGP 가 아니다.**
- kill test 는 자기 prereg 에 `"exploratory kill-test DGP, not the paper DGP"` 라고 적혀 있다.
- 이관 감사(`m5dataset_2026-08-07.md:71`)는 합성 study 를
  `length 576, train/val/test = 384/480/576` 으로 기록하는데
  `unified_temporal_27_v3` 는 512 / 358 / 409 다.
즉 H1/H2/H3 을 만든 세 번째 구성이 없는 것이다. 결과(Stage 1/3/4)는 어느 쪽도 로컬에 없다.

### 미검증으로 표시한 것

`_docs/history/2026-08-08.md` 에 다른 세션이 인용한 C01~C08 조건, 요인효과
`+7.83 / -4.58 / -6.26 / -16.74`(점추정만), magnitude 조작확인 `-0.73`,
18-cell rho sweep 은 **뒷받침 artifact 가 이 머신에 없다**. manifest 6절에
"인용됐으나 미검증" 으로 명시했고 paper_synthesis 어디에도 쓰지 않았다.

### 요청 목록 (manifest 4절)

우선순위 A: `~/Documents/github/m5dataset` 전체 (bundle + untracked 포함 archive).
remote 가 0개라 bundle 만으로는 working tree artifact 가 빠진다.
우선순위 B: `experiments/temporal_dependence/`, `experiments/decomposition_when_helps/`
두 디렉토리 전량 + `m5dataset/_docs/history/2026-08-01..05.md` + prereg/config/
Stage 1·3·4 결과표 + seed manifest + 생성기 테스트 (12항목).

상태: `SYNTHETIC_EVIDENCE_RECOVERY_READY_FOR_REVIEW`

---

## m5dataset recovery package 시도 — 이 머신에는 원본이 없음 (재확인)

recovery-package 프로토콜을 이 머신 기준으로 실행했으나 원본이 없다.
이번엔 프로토콜 자체 기준($HOME/Documents, $HOME, /home/<user>)으로 다시 찾았다.

```
uname                          MINGW64_NT-10.0-26200 DESKTOP-KVUSKN8 (Windows)
$HOME                          /c/Users/User
$HOME/Documents/github/m5dataset   없음
/home/minjae/...                   없음   (/home 자체가 없음)
WSL                            미설치 (wsl.exe 가 "설치되어 있지 않습니다" 반환)
로컬 .git 전수 (depth 5)        16개, m5dataset 없음
temporal_dependence /
decomposition_when_helps       0건
```

-> 이 머신 판정 `SYNTHETIC_SOURCE_NOT_FOUND`. 대체 repository 를 쓰지 않았고
`unified_temporal_27_v3`/`om_factorization_killtest` 를 paper DGP 로 오인하지 않았다.

### 대신 만든 것

`results/synthetic_source_recovery/make_recovery_package.sh` — 원본 머신에서 그대로
실행할 recovery 스크립트. 프로토콜 1~16단계 구현. `bash -n` 문법 검증 통과,
not-found 분기는 여기서 실제로 동작 확인. archive 경로는 대상이 없어 미실행이다.

핵심 안전장치: reset/clean/checkout/restore/commit/push/remote 생성 0건,
제외 파일 0건, archive 는 source repo **바깥** sibling `_recovery_exports/` 에 생성,
`.git`+untracked 전부 포함, untracked 파일이 archive 에 다 들어갔는지 개별 검증,
tar/bundle 각각 sha256 생성·검증, 종료 시 `git status` 전후 비교로 SOURCE_UNTOUCHED 확인.
paper DGP 오인 방지로 `"not the paper DGP"` 문구 grep 과 length-576/384-480-480 설정
grep 을 넣었다.

상태: `SYNTHETIC_SOURCE_TRANSFER_PACKAGE_READY` (스크립트 준비 완료, 원본 머신 실행 대기)
