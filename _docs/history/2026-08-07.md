# 2026-08-07

> 아래 "변경 없음" 절은 세션 전반부(리눅스 원본 머신) 기준이다. 같은 날 후반부에
> Windows 머신으로 저장소를 옮기면서 실제 변경이 발생했다 — 맨 아래 절 참조.

## 이 저장소(timeseries)의 코드/설정 변경: 없음

오늘 이 저장소에서 변경된 파일은 0건이다 (`find -newermt 2026-08-07` 로 확인).

오늘 작업은 다른 저장소(`~/Documents/github/m5dataset`)에서의 **읽기 전용 감사**였다 —
external-validity SCREEN 의 0단계 저장소 감사. 학습 0회, 파일 생성/수정 0건.
상세 기록은 `m5dataset/_docs/history/2026-08-07.md` 에 있다. 요지:

- 합성 study 의 Point/Hurdle DLinear 구현을 M5 에 재사용 가능함을 확인
  (`build_splits` 가 `y`, `z` 두 배열만 요구하므로 oracle 불필요)
- M5·Favorita 전처리 완료본이 각각 1200 계열로 존재. 사용자 자체 수요 데이터는 없음
  (`REAL_DATA_NOT_FOUND`)
- **충돌 3건 발견 후 실행 전 중단** — (a) M5 선행 zero 가 계열 332개에서 ADI·gap
  수열을 오염 (b) 합성 primary metric 이 oracle 기반이라 실데이터에 없음
  (c) horizon 24 대 28 불일치. 사용자 결정 대기 중.

## Stop hook 오탐 재발 기록 (3일째: 08-04, 08-05, 08-07)

Stop hook 이 사흘째 **똑같은 8개 파일**을 "이번 세션 변경"으로 지목했다.
실제로는 전부 **2026-08-01 수정분**이며 오늘 변경된 파일은 0건이다.

```
configs/study4_budgeted_acquisition.yaml
docs/study4_budget_audit.md
src/covariate_trust/acquisition_features.py
src/covariate_trust/acquisition_gates.py
src/covariate_trust/acquisition_models.py
src/covariate_trust/acquisition_reporting.py
src/covariate_trust/acquisition_value.py
src/covariate_trust/portfolio_selection.py
```

원인: 8개 모두 `git status` 에서 `??` (untracked). hook 이 mtime 이 아니라 **커밋 안 된
상태**를 변경으로 집계하는 것으로 보인다. 내용 기록은 `_docs/history/2026-08-01.md` 에
파일명·Before/After·이유까지 이미 있다. **중복 기록하지 않는다.**

해소 방법 두 가지 (둘 다 사용자 지시 대기):
1. Study 4 파일 8개를 커밋하면 `??` 가 사라져 오탐이 멈춘다.
2. `~/.claude/hooks/history-record-gate.py` 를 untracked 기준이 아니라 mtime 기준으로
   고친다.

3일 연속 같은 오탐이므로 둘 중 하나를 처리하는 편이 낫다.

---

## Windows 머신으로 clone + 전송 꾸러미 수령 (세션 후반)

### 배경

작업 환경을 리눅스 원본 머신(`/home/minjae/Documents/github/timeseries`)에서
Windows(`E:\CODING\proj\covariate-trust-pilot`)로 옮겼다. GitHub 에서 clone 하면
`.gitignore` 때문에 `data/` 가 비고, **미커밋 상태였던 Study 4 는 아예 따라오지 않는다.**
그래서 원본 머신에서 전송 꾸러미를 만들어 Downloads 경유로 옮겼다.

### 수령한 것

`Downloads/transfer-covariate-trust-pilot.zip` (23.6M). SHA256SUMS 4개 전부 OK,
zip 내 경로는 전부 저장소 루트 기준 상대경로이고 절대경로·`..` 탈출 0건.

```
01_data_minimal.zip     3.0M   data/processed/{series,favorita_series}.parquet, data/calendar.csv
02_data_m5_raw.zip       31M   data/{sell_prices,sales_train_evaluation}.csv  (해제 311M)
03_study4_recovery.zip   91K   Study 4 16개 파일
cli_py_697line.patch     35K   zip 의 cli.py 를 썼으므로 저장소에 풀지 않음
```

### data/ 검증 — 문서 기재값과 실측 일치

```
파일                             실측 bytes     shape          NaN  음수
series.parquet (M5)               2,421,954   (1200, 1941)     0    0
favorita_series.parquet           2,079,313   (1200, 1688)     0    0
calendar.csv                        103,469   1969 rows
sell_prices.csv                 203,395,785
sales_train_evaluation.csv      121,736,518
```

컬럼은 `m5_favorita_data_derivation.md` §4 와 동일
(`series_id, timestamp, target, item_id, dept_id, category_id, store_id, state_id`).
NaN·음수 0건이라 `screen.py::load_dataset` 의 `ScreenFailure` 검사를 통과할 조건은 충족.

### Study 4 복구 — 커밋 `d6a5af5` (브랜치 `recover-study4`)

16개 파일, +3497/-0. 앞선 history 들이 "8개"로 적어둔 것보다 많다 — `tests/` 5개와
`docs/study4_budget_{estimand,preregistered}.md` 2개가 빠져 있었다.

```
configs/  1   docs/  3   src/covariate_trust/  6   tests/  5   cli.py(+697/-0)  1
```

`PYTHONPATH=src pytest tests/test_acquisition_*.py tests/test_portfolio_selection.py`
→ **46 passed, 1 skipped**.

부수 효과: 08-04·08-05·08-07 사흘 연속 발생하던 Stop hook 오탐이 멈춘다.
원인이 정확히 이 `??` (untracked) 상태였다.

### 이관 문서의 오류 하나 정정

`2026-08-07-migration.md` 와 `external_validity_STATUS.md` 가 "Stage A 재현에는
4.4M(parquet 2개 + calendar) 만 있으면 된다" 고 적었는데 **틀렸다.**

```
screen.py:30-32   DATASETS["m5"]["leading_zero"] = "availability_aware"
screen.py:79-81   if spec["leading_zero"] == "availability_aware": m5_availability(...)
screen.py:47      pd.read_csv(REPO / "data" / "calendar.csv", ...)
screen.py:50      pd.read_csv(REPO / "data" / "sell_prices.csv", ...)
```

M5 는 `load_dataset` 마다 `sell_prices.csv` (194M) 를 무조건 읽고 캐시도 없다.
즉 **Stage A 에도 02 꾸러미가 필요**하다. 4.4M 만으로 되는 것은 Favorita 뿐이다.

### 이 머신의 환경 상태

```
GPU              RTX 4070 12GB, driver 595.79
numpy 2.3.5 / pandas 2.3.3 / pyarrow 21.0.0 / scipy 1.16.3 / matplotlib 3.10.6 / yaml 6.0.3
torch            없음  -> posthoc 조차 실행 불가
core.autocrlf    true  (리눅스 원본이 LF, 작업트리는 CRLF. 커밋 내용은 LF 유지)
```

`posthoc.py` 는 재학습을 안 하지만 `from . import screen` 하고 `screen.py:16` 이
모듈 최상단에서 `import torch` 하므로 torch 없이는 import 단계에서 죽는다.
따라서 README 의 재현 검증(m5 H1 +0.1065 등)은 **아직 미실행**이다.

### posthoc 재현 검증 — 통과 (헤드라인 수치 비트 동일)

torch 설치 후 `python -m experiments.external_validity_screen.posthoc` 실행 (exit 0,
`stage_a_unmodified=True`). 커밋본(리눅스 원본 머신 산출)과 전체 JSON 을 재귀 비교했다.

```
구조 차이      0
비수치 차이    8   전부 stage_a_integrity 의 sha256  -> CRLF 로 설명됨 (아래)
수치 차이     66   그중 8개는 integrity 의 bytes     -> CRLF
                  나머지 58개 최대 상대오차 1.546e-06
```

**헤드라인 수치는 부동소수점 비트까지 동일하다.**

```
                        committed              rerun                  판정
m5       H1 raw          0.10645816734905524   0.10645816734905524    동일
         H1 relative     0.12308621758151522   0.12308621758151522    동일
         BSS pooled     -0.0084439985714857   -0.0084439985714857     동일
         full-pool       714 / control 5142    714 / 5142             동일
favorita H1 raw          0.07891335260963367   0.07891335260963367    동일
         H1 relative     0.11782415597915766   0.11782415597915766    동일
         BSS pooled     -0.09078264444704032  -0.09078264444704032    동일
         full-pool        35 / control  115     35 / 115              동일
```

#### 차이 8건(sha256/bytes)의 원인 = core.autocrlf 의 CRLF 변환

바이트 증가분이 파일 줄 수와 **4/4 정확히 일치**한다. 데이터 훼손이 아니다.

```
파일                     줄수     bytes 증가
gate_report.json           98     +98
per_series_metrics.csv   2401     +2401
pre_analysis_spec.json    115     +115
stage_a_results.json      472     +472
```

> 함의: 이 저장소의 `stage_a_integrity_*` 해시는 **줄바꿈 정책에 종속적**이라
> Windows 체크아웃에서는 원본 머신 해시와 절대 일치하지 않는다. 무결성 검사를
> 크로스 플랫폼으로 쓰려면 `.gitattributes` 에 `* text=auto eol=lf` 를 넣어야 한다.
> (아직 넣지 않았다 — 기존 커밋 전체의 줄바꿈에 영향을 주는 변경이라 사용자 판단 필요.)

#### 나머지 58건(최대 1.5e-06)의 소재

전부 `h1_confound` (OLS 계수·CI) 와 `h2_full_pool` 의 descriptor 요약통계
(`rho_interval_abs_train`, `rho_magnitude_train`) 에만 나타난다. 리눅스와 Windows 의
BLAS/LAPACK 구현 차이에서 오는 수준이다. `h1_confound` 는 status board 가 이미
`[탐색]` 으로 표시한 항목이라 결론에 영향 없다.

검증 후 `git checkout` 으로 커밋본을 원복했다 — 재실행 산출물은 CRLF 유래 해시와
1e-6 잡음을 담고 있어 동결된 기록을 덮어쓸 이유가 없다.

### torch 설치 (anaconda base) — OMP 충돌과 해소

```
torch 2.13.0+cu126 (2.6GB), CUDA True, RTX 4070 인식
numpy 2.3.5 / pandas 2.3.3 / scipy 1.16.3  — 설치 전후 동일, 기존 패키지 오염 없음
```

`import torch` **단독은 실패**한다:

```
OMP: Error #15: Initializing libiomp5md.dll, but found libiomp5md.dll already initialized.
```

anaconda 의 MKL(`anaconda3/Library/bin/libiomp5md.dll`)과 torch 자체
(`site-packages/torch/lib/libiomp5md.dll`)가 각각 OpenMP 런타임을 들고 온다.

```
import torch          FAIL
import numpy          OK
import numpy, torch   OK     <- numpy 가 먼저면 통과
import torch, numpy   FAIL
```

`posthoc.py` 는 `numpy` -> `pandas` -> `scipy` -> `screen`(torch) 순으로 import 하므로
그대로 통과한다. **`KMP_DUPLICATE_LIB_OK=TRUE` 는 쓰지 않았다** — 그 옵션은 Intel 이
"silently produce incorrect results" 가능성을 경고하는 것이라, 수치 재현을 검증하는
작업에 쓰면 검증 자체가 무의미해진다.

> 주의: torch 를 numpy 보다 먼저 import 하는 새 스크립트를 쓰면 죽는다.
> 근본 해결은 README 의 `.venv` 분리이며, 지금은 import 순서로 회피 중이다.

---

## SBC regime H1 + M5 H2 independent confirmatory — matching gate 에서 정지

### 무엇을 했나

새 Windows/Anaconda 환경에서 학습 entry point 안전성을 확인한 뒤(0단계),
기존 Stage A 결과를 재실행하지 않고 두 가지를 진행했다.
Stage B 3-seed 와 Favorita 는 이번 범위에서 제외.

추가 파일 2개:

```
experiments/external_validity_screen/regime_h1.py        SBC regime 별 H1 (재학습 0회)
experiments/external_validity_screen/confirmatory_h2.py  독립 M5 H2 confirmatory
results/external_validity_screen/regime_h1/              regime H1 산출물
results/external_validity_screen/h2_confirmatory/        matching_failed.json 만 (학습 미실행)
```

### 0단계 Training environment Gate — TRAIN_ENV_PASS

11개 항목 전부 통과. `KMP_DUPLICATE_LIB_OK` 미사용, checkpoint 미생성, H2 예측 미생성.

```
torch 2.13.0+cu126, CUDA True, RTX 4070
Point  M0PM_point_mse_param_matched   7056 params  forward/loss/backward/step OK  non-finite 0
Hurdle M1_factorized_mean             7056 params  forward/loss/backward/step OK  non-finite 0
```

import chain 을 추정하지 않고 소스와 독립 프로세스 양쪽으로 확인했다.
`unified_temporal_27_v3/model.py:7` 만 numpy 없이 torch 를 import 해서 **단독 import 시
OMP Error #15 로 죽는다.** 다만 실행 경로에서는 `om_factorization_killtest/models.py`
(numpy 25행 -> torch 26행 -> model 30행) 와 `training.py:39` 를 통해서만 도달하므로
항상 numpy 이후다. **코드 수정 불필요**, 제약은 "새 entry point 는 numpy 를 먼저
import" 뿐이고 저장소 기존 관례와 같다.

### [충돌] SBC regime label 이 저장소에 없다 -> 검증으로 해소

`per_series_metrics.csv` 에도 `series.parquet` 에도 regime 컬럼이 없다. label 은 이관되지
않은 `prepare_m5.py` 산물이다. 지시받은 "기존 label 을 그대로 사용"은 문자 그대로 불가능.

유도 문서 §4 의 동결 규칙(ADI 1.32 / CV2 0.49, min_positive 2, min_len 184)을 raw
train 구간에만 적용해 재계산하고, **층화 설계와 대조해 복원 여부를 판정**했다.

```
                 재계산     유도문서 기재
smooth              984            984
erratic             496            496
intermittent     23,053         23,053
lumpy             5,942          5,942
excluded             15             15
Stage A 1200   300/300/300/300   각 300
```

6개 지표 전부 일치 -> `REGIME_LABEL_REPRODUCED`. 추측이 아니라 검증된 복원이다.

### 2단계 결과 — H1_TARGET_REGIME_DIRECTION_SUPPORTED

x = `rho_interval_abs_train`, pool = n_positive_train >= 20 (1200 전부 통과),
bootstrap 2000 draws, unit = series, seed 20260807. 재학습 0회.

```
delta_relative   N     rho        95% CI              mean     Point win
smooth          300  +0.0872  [-0.0319, +0.2084]    -0.0252     70.7%
erratic         300  +0.0882  [-0.0246, +0.1984]    +0.0139     55.7%
intermittent    300  +0.1529  [+0.0519, +0.2613] *  +0.0450     33.3%
lumpy           300  +0.0323  [-0.0793, +0.1463]    +0.0141     51.7%
```

intermittent 와 lumpy 둘 다 rho > 0 -> 판정 SUPPORTED. 다만 CI 가 0 을 제외하는 것은
intermittent 뿐이고 lumpy 는 사실상 0 근처다. delta_raw 에서는 smooth 만 CI 가 0 을
제외하는데 relative 에서는 intermittent 로 옮겨간다 — scale 정규화가 어느 regime 이
신호를 만드는지를 바꾼다.

### 3단계 frozen H2 cutoff — 비트 단위 복원

`posthoc_diagnostic.json > datasets.m5.frozen_cutoffs_recovered` 에서 읽고, 동일 Stage A
pool(n=1200)에서 `posthoc.frozen_cutoffs` 재실행분과 대조했다. full M5 재계산 아님.

```
HIGH_ADI_min        1.3036350677120456
LOW_OCC_max         0.0074430844292528
MAG_PERSISTENT_min  0.290998278399666
```

### 4단계 독립 population — 내적 정합성 확인됨

```
M5 전체                    30,490
descriptor eligible        30,406      (posthoc 기재 30,406 과 일치)
Stage A 1200 제거 후       29,206
candidate                     675
control pool                5,018
Stage A overlap                 0
```

**posthoc 의 full-pool candidate 714 에서 Stage A 의 39 를 빼면 정확히 675.**
population 구성이 맞다는 강한 증거다.

### 5단계 matching — SMD_FAIL, 학습 전 정지

train-only 변수 4개(log ADI / positive CV2 / log train_scale / abs rho_interval)로
1:2 greedy nearest neighbour, seed 20260807. rho_magnitude 는 candidate/control 을
가르는 변수라 matching 에서 제외.

```
log_ADI           -0.0077
CV2_positive      +0.1336
log_train_scale   +0.6136   <- FAIL
abs_rho_interval  -0.1465
```

`|SMD| > 0.20` 이므로 **학습을 시작하지 않고 중단**했다. spec 은 freeze 되지 않았고
예측도 생성되지 않았다.

#### 원인은 알고리즘이 아니라 support (positivity) 이다

control pool 이 candidate 의 7.4배라 표본 부족이 아니다. 진짜 원인:

```
rho_magnitude ~ log_train_scale 상관 = +0.5754   (전체 eligible)
rho_magnitude ~ log_ADI         상관 = -0.4035

log_train_scale 중앙값   candidate 0.8803   control pool 0.0000
candidate 최상위 10분위 [2.10, 4.21]:  candidate 68개  control 4개  (비 0.06)
```

**MAG_PERSISTENT 조건이 계열 규모와 구조적으로 얽혀 있다.** candidate 상위 분위에는
짝지을 control 이 물리적으로 없다. Stage A 는 candidate 가 39개뿐이라 이 문제가
드러나지 않았다.

#### 조정 여지 (공변량만 계산, freeze/학습 없음)

```
 k  caliper  matched cand  ctrl   worst |SMD|
 1      inf           675   675   0.2995 FAIL
 1     0.50           594   594   0.0989 PASS
 1     0.25           584   584   0.0930 PASS
 1     0.10           580   580   0.0808 PASS
 2      inf           675  1350   0.6136 FAIL
 2     0.50           508  1016   0.1231 WARN
```

1:1 + log_train_scale caliper 면 후보의 86~88% 를 유지하면서 SMD_PASS 가 된다.
**caliper 는 사전 지정에 없던 규칙이라 적용하지 않았다.** 어떤 test 지표도 보지 않은
상태이므로, 지금 프로토콜을 개정하는 것은 여전히 outcome-blind 다.

### 게이트

```
G0  training environment            PASS
G1  기존 Stage A/Posthoc hash 불변   PASS  8개 파일 전부 실행 전후 동일
G2  Stage A series overlap = 0      PASS
G3  frozen cutoff 재계산 없음        PASS  artifact 에서 읽고 동일 pool 로 대조만
G4  selection/matching train-only   PASS  test 지표 미사용
G5  matching SMD <= 0.20            FAIL  0.6136
G6  Point/Hurdle protocol 동일       N/A   학습 미실행
G7  bootstrap unit = series         PASS  regime H1 에 적용
G8  raw prediction 저장 무영향       N/A   학습 미실행
G9  결과 보고 규칙 변경 없음          PASS
G10 새 test failure 없음            PASS  272 passed, 5 skipped
```

G5 critical FAIL 이므로 **H2 에 대한 과학적 결론을 내지 않는다.**

상태: `EXTERNAL_VALIDITY_H2_CONFIRMATORY_BLOCKED_ON_MATCHING`

---

## H2 를 Primary(rule 외적검증) / Secondary(overlap 진단) 두 층으로 분리해 완주

직전 matching 실패를 지우지 않고 보존한 채, 질문을 둘로 나눴다.
`h2_confirmatory/matching_failed.json` 은 그대로 두고 `rule_replication/` 을 새로 만들었다.

### 설계 수정의 근거

candidate 를 정의하는 `rho_magnitude` 가 실제 M5 에서 `log_train_scale` 과 +0.575 로
얽혀 있다는 사실이 outcome 을 보기 전에 확인됐다. 이 상관은 real-data joint distribution
의 일부이므로, matching 으로 제거한 population 만 평가하면 **frozen rule 의 외부 선택력을
검증하는 질문 자체가 달라진다.** 따라서 matching 을 primary 의 선행 gate 에서 내렸다.

### 3단계 model provenance — checkpoint 없음 -> B분기, 그런데 재현이 사실상 완전

```
.pt/.pth/.ckpt          0개
runs/                   .gitkeep 만
cli.py:187              best_state 를 메모리에만 보관
torch.save              training.py:1056 한 곳뿐 — SCREEN 실행경로 아님
```

Stage A 1200 계열로만 재학습해 기존 artifact 와 대조했다. 허용오차는 **실행 전에**
`REPRODUCTION_TOLERANCE` 로 코드에 고정했다 (rank Spearman >= 0.80 이 binding check —
H1/H2 가 계열 순위에 관한 주장이므로 순위를 바꾸는 모델은 같은 모델이 아니다).

```
                          허용        실측
rmse_point 상대차         0.02        2.15e-09
rmse_hurdle 상대차        0.02        1.68e-09
point_win_pct 절대차      5.0         0.0
per-series delta Spearman >= 0.80     1.0
```

Stage A 는 Linux torch 2.5.1+cu121, 이번은 Windows torch 2.13.0+cu126 인데도
**순위가 완전히 일치**한다. 재현된 모델을 freeze 해 checkpoint 로 저장했고
(`rule_replication/models/{point,hurdle}.pt`), independent 계열에는 예측만 수행했다.

### 4~6단계 PRIMARY — H2_RULE_PARTIAL_REPLICATION

matching 없음. candidate 675 전부, control 5018 전부 (샘플링 없음).

```
                        candidate      control
N                             675         5018
mean delta_relative       +0.0013      +0.0243
median delta_relative     -0.0097      +0.0029
mean delta_raw            -0.0455      +0.0037
Point win rate             58.96%       47.09%
mean 95% CI        [-0.0045,+0.0076] [+0.0223,+0.0265]

H2_rule_effect            -0.023039   CI [-0.02942, -0.01634]  0 제외
Point win rate 차이        +0.1187     CI [+0.0785, +0.1581]   0 제외
```

STRONG 4개 조건 중 3개 충족. 걸린 것은 **candidate mean delta_relative 가 0 미만이
아니다**(+0.0013, CI 가 0 포함). 다만 median 은 -0.0097, mean delta_raw 는 -0.0455 로
둘 다 음수다 — 평균만 양수인 것은 delta_relative 의 우측 꼬리 때문이다.

읽는 법: **rule 은 selector 로서 결정적으로 작동한다**(Point 승률 +11.9%p, CI 0 제외).
그러나 선택된 계열이 절대적으로 Point 우위인 것은 아니고 상대 지표 평균으로는 무승부다.

### 8단계 SECONDARY — OVERLAP_ADJUSTED_ASSOCIATION (causal 아님)

train-only 공변량 4개로 logistic propensity, overlap(ATO) weights.
`rho_magnitude` 는 대조를 정의하므로 조정변수에서 제외.

```
변수                unweighted SMD   weighted SMD
log_ADI                    -0.8976        +0.0001
CV2_positive               +0.7265        -0.0004
log_train_scale            +1.3193        +0.0002
abs_rho_interval           -0.2257        -0.0001
worst |SMD|                 1.3193         0.0004

effective sample size   candidate 561.2 / 675   control 1545.1 / 5018
propensity 중앙값       candidate 0.2936   control 0.0411

OVERLAP_ADJUSTED_ASSOCIATION  +0.003185   CI [-0.003289, +0.009384]  0 포함
```

균형은 거의 완벽하게 맞았고(worst 0.0004), 그 상태에서 **차이가 사라진다**
(-0.0230 -> +0.0032, CI 0 포함).

**Primary 를 부정하지 않는다.** overlap weighting 은 두 군이 실제로 공존하는 저-scale
영역으로 사실상 제한한다(control ESS 가 5018 -> 1545 로 31%). 즉 secondary 가 답하는
것은 "overlap 영역 안에서 rho_magnitude 자체가 기여하는가"이고, 답은 "확인되지 않음"이다.

해석: frozen rule 의 선택력은 실데이터에서 **rho_magnitude 고유 효과가 아니라 그것이
얽혀 있는 공변량(주로 scale, ADI)을 통해 작동하는 것으로 보인다.** selector 로서의
유용성과 mechanism 주장은 별개다.

### 9단계 구조적 불일치 — 별도 empirical finding

```
rho_magnitude ~ log_train_scale   +0.5754
rho_magnitude ~ log_ADI           -0.4035
unweighted SMD log_train_scale    +1.3193 (candidate vs control)
```

synthetic 에서 독립적으로 조작한 축들이 real M5 에서는 독립이 아니다.
이 상관을 Point/Hurdle 성능의 원인으로 단정하지 않는다.

### 10단계 SBC H1 — 재계산 없음, 표현만 확정

intermittent relative rho +0.153 (CI 0 제외) -> external support.
lumpy +0.032 (CI 0 포함) -> 방향은 양수지만 불확실.
**"intermittent 에서 재현됐고 lumpy 에서는 불확실"** 로 쓴다.
"둘 다 확정 재현"이라고 쓰지 않는다.

### 게이트

```
G0  training environment            PASS
G1  기존 artifact hash 불변          PASS  21항목(파일 20 + Stage A ID manifest) 전부 동일
G2  Stage A overlap = 0             PASS  freeze·scoring 양쪽에서 확인
G3  frozen H2 cutoff 불변            PASS  artifact 에서 읽음, full pool 재계산 없음
G4  independent 계열 학습 미사용      PASS  checkpoint trained_on=stage_a_m5_1200
G5  selection 은 train-only          PASS
G6  Point/Hurdle 동일 frozen config  PASS  seed 0, lookback 96, horizon 28, 동일 TRAINING
G7  primary 에 matching 미적용        PASS  spec.primary.matching_applied=False
G8  primary/secondary 분리           PASS  파일·판정·명칭 분리
G9  bootstrap unit = series          PASS
G10 raw logging 이 prediction 불변    PASS  predict() 반환값을 reshape 만
G11 결과 보고 rule 변경 없음          PASS
G12 새 test failure 없음             PASS  272 passed, 5 skipped
```

freeze 2026-08-07T12:01:48Z < scoring 12:01:55Z 로 순서도 확인됐다.

상태: `EXTERNAL_VALIDITY_H2_RULE_REPLICATION_READY_FOR_REVIEW`

---

## Priority 1/2/3 연속 실행 — seed robustness, Favorita transfer, classical benchmark

추가 모듈 3개, 산출물은 전부 `results/external_validity_screen/` 하위 신규 폴더.
기존 frozen artifact 는 하나도 건드리지 않았다 (33항목 봉인 후 대조, 전부 동일).

```
experiments/external_validity_screen/seed_robustness.py       P1
experiments/external_validity_screen/favorita_transfer.py     P2
experiments/external_validity_screen/classical_benchmark.py   P3
results/external_validity_screen/empirical_validation_summary.json
```

### 0단계 감사에서 나온 [충돌] 과 이름 오류

1. **Favorita full pool 이 없다.** `data/processed/favorita_series.parquet` 은 Stage A
   1200 계열 그 자체이고 raw Favorita 원본은 저장소에 없다. posthoc 이 WARN 으로 적어둔
   "Favorita full pool 미물질화" 가 여기서 실제 blocker 가 됐다.
   Downloads 의 `store-sales-time-series-forecasting.zip` 은 **대체 불가** —
   `family` 단위(33 x 54)이고 저장소는 `item_nbr_store_nbr` 단위다. 쓰면 새 preprocessing
   기준을 만드는 셈이라 금지 조항에 걸린다.

2. seed convention 을 `km_prereg.SEEDS` 로 추정했다가 틀렸다. 실제는
   **`km_prereg.DATA["model_seeds"] = (0, 1)`**. 코드 확인 후 `(0,1,2)` 로 확정했다.

### P1 — H2_SEED_ROBUST

seed 0 은 단일-seed replication 이 쓴 checkpoint 를 그대로 재사용(재학습 없음),
seed 1·2 만 Stage A 1200 으로 학습. 평가는 frozen H2 population 675/5018 고정.

```
seed    cand mean  ctrl mean   effect       effect 95% CI   cand win  ctrl win  win diff
0         +0.0013    +0.0243  -0.0230  [-0.0294, -0.0163]     59.0%     47.1%   +11.9p
1         +0.0064    +0.0275  -0.0211  [-0.0278, -0.0141]     54.2%     44.9%    +9.3p
2         +0.0022    +0.0299  -0.0277  [-0.0344, -0.0205]     58.5%     43.9%   +14.6p
3-seed    +0.0033    +0.0272  -0.0239  [-0.0305, -0.0172]     59.7%     46.2%   +13.5p
```

3개 seed 전부 음수, 개별 CI 도 각각 0 제외, aggregate CI [-0.0305,-0.0172] 0 제외.
seed 를 추가 표본으로 취급하지 않았다 — 계열별로 3 seed 평균을 먼저 내고 계열 단위
bootstrap 했다.

### P2 — FAVORITA_RULE_LOW_SUPPORT (지정된 설계를 실행하지 못함)

지정된 P2(full pool 에서 Stage A 1200 제거)는 **데이터 부재로 실행 불가**. 대신 M5 frozen
cutoff 를 숫자 그대로 Favorita Stage A 1200 에 적용한 **축소판**을 돌리고 그렇게 표시했다.
cutoff 는 Favorita 로 재적합하지 않았다 (Favorita 자체 분위는 abs rho_I lower tertile
0.0088 vs M5 0.0074 로 달라서, 재적합했다면 다른 표본이 나온다).

Favorita Stage A 재현 먼저: Spearman 0.99999, RMSE 상대차 1e-09 이므로 REPRODUCED.

```
                      candidate      control
N                            18          117
mean delta_relative     +0.0312      +0.0686
median delta_relative   +0.0169      +0.0061
Point win rate            33.3%        47.9%

H2_FAVORITA_rule_effect  -0.0374   CI [-0.0853, +0.0154]  0 포함
Point win rate 차이       -0.1453
```

**평균 기준 방향은 M5 와 같은 음수인데 Point 승률 차이는 반대 부호다.** n=18 에서 평균이
소수 계열에 끌린 것으로 보이며, 두 지표가 어긋나므로 방향 일치를 근거로 쓰면 안 된다.
secondary overlap 은 candidate 18개로 4-공변량 propensity 가 식별되지 않아
OVERLAP_DIAGNOSTIC_INCONCLUSIVE.

occurrence diagnostic 은 계산됐다 (candidate BSS 0.2046 / ROC-AUC 0.7808,
control BSS 0.1177 / ROC-AUC 0.7693) — Stage A 가 관측치 단위 p_hat 을 저장하지 않아
못 냈던 ROC-AUC/PR-AUC WARN 이 여기서 해소된다.

### P3 — CLASSICAL_BENCHMARK_COMPLETE

TSB 와 seasonal_naive 는 저장소 구현(`km_models.tsb_state`, `seasonal_naive`)과 frozen
`TSB_GRID` 를 재사용. Croston/SBA/SES/naive 는 저장소에도 환경에도 없어 직접 구현했고,
**hand_check 7/7 통과 후에만** 벤치마크에 투입했다 (손계산 z=3.25, x=2.75,
croston=1.1818, sba = 0.75 x croston 과 소수 12자리까지 일치). iETS 는 검증된 구현이
없어 `IETS_NOT_AVAILABLE` 로 두고 전체를 막지 않았다.
smoothing parameter 는 전부 validation split 에서만 선택, grid 는 TSB 것 재사용.

```
M5 (n=1200)        overall RMSE  mean/series  median/series  mean rank   win%
tsb                      3.8922       2.8382         2.0937       3.41  22.6
ses                      3.9066       2.8449         2.1049       3.48  11.0
sba                      3.9514       2.8730         2.1117       3.15  19.0
croston                  3.9880       2.8904         2.1135       3.26  11.8
dlinear_point            4.0321       2.9209         2.1454       4.20  17.8
dlinear_hurdle           4.0373       2.9215         2.1318       4.22  13.5
seasonal_naive           4.9818       3.7022         2.8442       7.45   0.4
naive                    5.4443       3.8516         2.7201       6.82   3.8

Favorita (n=1200)  overall RMSE  mean/series  median/series  mean rank   win%
dlinear_point           20.6833       5.1124         2.3556       3.95  25.5
sba                     21.1063       5.1270         2.3180       3.07  20.7
dlinear_hurdle          20.8086       5.1443         2.3464       4.03  15.7
ses                     21.6412       5.1803         2.3245       3.94  10.2
tsb                     21.2454       5.2007         2.2971       3.80   9.8
croston                 21.4087       5.2365         2.3465       3.79  10.2
naive                   22.6708       6.0351         2.8326       6.31   4.6
seasonal_naive          23.0352       6.2504         3.0375       7.12   3.4
```

Friedman: M5 chi2=3974.9, Favorita chi2=2852.5, 둘 다 p < 1e-300, unit=series, k=8.
post-hoc 은 pairwise Wilcoxon signed-rank + Holm — 저장소에 표준 post-hoc 이 없고
Nemenyi 제공 패키지도 설치돼 있지 않아 scipy 로 가능한 표준 절차를 썼다.

**M5 에서는 classical 4종이 학습 모델 둘을 모두 앞선다.** mean rank 로는 SBA 가 양쪽
데이터셋 모두 1등(3.15 / 3.07)인데 mean RMSE 순위와 어긋난다 — 평균이 소수의 큰 계열에
끌리는 반면 rank 는 계열마다 1표씩이기 때문이다. 이 결과는 H1/H2 mechanism 의 증거로
쓰지 않는다.

### 게이트

```
P1-G1~G8   전부 PASS
P2-G1~G11  G1/G2 PASS (M5 cutoff 그대로, Favorita 재적합 없음)
           지정된 full-pool 요건은 데이터 부재로 미충족 -> 축소판임을 산출물에 명시
P3-G1~G9   전부 PASS (구현 provenance 명시, 동일 series 와 test period,
           test tuning 없음, 실패 method 없음, unit=series)
공통       기존 artifact 33항목 hash 불변 / Stage A 와 H2 overlap 0 /
           frozen cutoff 불변 / 모든 checkpoint trained_on 이 Stage A 전용 /
           pytest 272 passed, 5 skipped
```

전체: `EMPIRICAL_VALIDATION_PARTIAL` — M5 는 seed 에 견고하나 Favorita 는 지정된 형태로
검정하지 못했고 축소판은 LOW_SUPPORT.

상태: `EMPIRICAL_VALIDATION_3PRIORITIES_READY_FOR_REVIEW`

---

## Favorita full pool 복원 — P2 를 지정된 설계대로 재실행

### 막혀 있던 것

P2 는 "Favorita full pool 에서 Stage A 1200 을 빼고 M5-frozen rule 적용"인데,
저장소에 있던 Favorita 는 그 Stage A 1200 자체뿐이라 남는 게 0 이었다.
그래서 축소판(Stage A 내부, candidate 18)으로 돌려 LOW_SUPPORT 로 끝났었다.

사용자가 Kaggle 원본을 받아주기로 해서 데이터를 확보했다.

### 인증 — 레거시 방식이 안 먹혔다

`KGAT_` 로 시작하는 신형 토큰은 기존 `kaggle.json` Basic 인증으로 401 이다.
5가지 방식을 실제로 시험해 통과하는 것을 찾았다.

```
Basic user:key          401
Bearer key              200   <- 이것
Basic key-only          401
X-Kaggle-ApiToken       401
Basic user:key(no pfx)  401
```

CLI 설치를 기다리지 않고 API 를 직접 호출했다.
받은 것: `train.csv.7z` 474,092,593 B, `items.csv.7z`, `stores.csv.7z`.
해제하면 `train.csv` 4,997,452,288 B.

### 관문 1 — 목표 변환 규칙 복원 (prepare_favorita.py 없이)

Favorita raw 는 **0 판매일의 행이 아예 없고** `unit_sales` 는 실수이며 음수(반품)도 있다.
저장소 parquet 은 int32 비음수 조밀격자다. 그 변환 코드가 이관되지 않았다.

후보 6개를 세워 저장소의 알려진 1200 계열과 전수 대조했다.

```
clip0_then_round     불일치          0 / 2,025,600   <- 채택
round_then_clip0     불일치          0 / 2,025,600   (동치)
clip0_then_floor     불일치     29,607
clip0_then_trunc     불일치     29,607
floor_then_clip0     불일치     29,607
trunc_then_clip0     불일치     29,607
```

**규칙: `clip(unit_sales, 0, None)` -> `rint`, 결측 행 = 0.**
2,025,600 셀 전부 일치하므로 추정이 아니라 복원이다.

### 관문 2 — 계열 선정 규칙 복원

유도문서 §4 가 "eligible 56,918 -> 1,200" 을 기록해 뒀다. 이게 검증 목표다.

```
raw (item,store) 쌍          174,685
first_day <= 90 (full-life)   56,966
+ SBC 자격(min_pos 2/min_len 184)  56,918
유도문서 기재                  56,918   <- 정확히 일치
```

M5 때 regime label 을 300x4 로 검증한 것과 같은 방식이다.
산출물: `data/processed/favorita_full_pool.parquet` (56,918 x 1688, 79.5MB).

메타 매핑도 미리 확인했다 — 1200 계열 전수에서
`category_id == items.family`, `dept_id == items.class`, `state_id == stores.state`, 조인 실패 0.

### P2 정식 실행 — FAVORITA_RULE_PARTIAL_TRANSFER

```
full pool 56,918 -> eligible 56,767 -> Stage A 1195 제거 -> 55,572
candidate 792 / control 4,613 / overlap 0
```

model 은 dataset 간 transfer 하지 않는다. Favorita Stage A 1200 으로 재현·동결한
Favorita 자체 Point/Hurdle 를 쓰고, cutoff 3개만 M5 것을 읽어 쓴다.

```
                             candidate       control
N                                  792         4,613
mean delta_relative            +0.0459       +0.0716
median delta_relative          -0.0010       +0.0041
mean delta_raw                 +0.0297       +0.0620
Point win rate                   51.1%         47.2%

H2_FAVORITA_rule_effect  -0.0256   CI [-0.0370, -0.0133]   0 제외
Point win rate 차이       +0.0390   CI [+0.0030, +0.0747]   0 제외
```

STRONG 4조건 중 3개 충족, 걸린 것은 candidate mean delta_relative 가 0 미만이 아님
(+0.0459). M5 와 정확히 같은 패턴이다.

### M5 대 Favorita — pooling 하지 않고 나란히

```
                                    M5              Favorita
candidate / control          675 / 5018            792 / 4613
PRIMARY effect                  -0.0230               -0.0256
  CI 0 제외                        True                  True
  Point win 차이                 +0.1187               +0.0390
SECONDARY overlap-adjusted      +0.0032               -0.0451
  CI                   [-0.0033, +0.0094]   [-0.0581, -0.0325]
  CI 0 제외                       False                  True
```

**Primary 는 두 데이터셋에서 재현된다.** effect 크기가 거의 같고 둘 다 CI 가 0 을 제외하며
Point 승률 차이도 둘 다 양수에 CI 가 0 을 제외한다.
앞선 축소판(n=18)에서 승률 차이가 반대 부호였던 것은 **표본 부족이 원인이었음이 확인**됐다.

**Secondary 는 두 데이터셋에서 반대다.** M5 는 overlap 조정 후 사라지는데(+0.0032, CI 0 포함)
Favorita 는 남을 뿐 아니라 커진다(-0.0256 -> -0.0451, CI 0 제외).
균형은 거의 완벽하다 (worst weighted |SMD| 0.0006, ESS 762/3192).

> 그러므로 이전에 M5 만 보고 적은 "rule 의 효과는 얽힌 공변량 때문이고 magnitude
> persistence 단독 기여는 확인되지 않는다"는 해석은 **M5 에 한정된 것**이며 일반 결론으로
> 쓰면 안 된다. Favorita 에서는 조정 후에도 남는다.

### 게이트

```
P2-G1 M5 frozen cutoff 그대로        PASS
P2-G2 Favorita cutoff 재적합 없음     PASS  (자체 분위는 참고용 기록만)
P2-G3 descriptor train-only          PASS
P2-G4 Stage A 제외 / overlap 0        PASS  (1195 제거)
P2-G5 independent 계열 학습 미사용     PASS  (trained_on=stage_a_favorita_1200)
P2-G6 Point/Hurdle 동일 protocol      PASS  (seed 0, lookback 96, horizon 28)
P2-G7 Primary/Secondary 분리          PASS
P2-G8 bootstrap unit = series        PASS
P2-G9 raw logging 무영향              PASS
P2-G10 rule 변경 없음                 PASS
공통  기존 artifact 29항목 hash 불변   PASS   Stage A ID manifest 2종 불변 PASS
      freeze < scoring 순서            PASS   pytest 272 passed, 5 skipped
```

전체 판정을 `EMPIRICAL_VALIDATION_PARTIAL` -> **`EMPIRICAL_VALIDATION_STRONG`** 으로
갱신했다. 이전 PARTIAL 은 부정적 결과 때문이 아니라 Favorita 를 검정할 수 없었기
때문이었고, 그 원인이 해소됐다.

`favorita_transfer` (18 candidate, LOW_SUPPORT) 는 **지우지 않고 보존**한다.
두 결과를 나란히 볼 수 있어야 표본 부족이 무엇을 했는지 읽을 수 있다.

상태: `EMPIRICAL_VALIDATION_3PRIORITIES_READY_FOR_REVIEW`
