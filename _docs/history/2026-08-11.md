# 2026-08-11

## 합성 study 원본 회수 완료 — C1_CONFIRMED, PAPER_READY_FOR_OUTLINE

새 실험·학습·TEST 채점 0회. commit/push 하지 않았다. `results/paper_synthesis/` 미수정.
신규 `results/synthetic_source_verification/`(16개), `results/paper_synthesis_verified/`(2개).

### 전송

Ubuntu(minjae-MS-7D17)에서 만든 recovery package 를 GitHub Private Release 로 받았다.

```
repo     CanelE452/m5dataset-recovery   PRIVATE
release  recovery-20260811_135203       asset 19개 전부 uploaded
경로     E:\research_recovery\m5dataset_recovery-20260811_135203\{download,reconstructed,extracted}
```

무결성 전 게이트 통과:

```
parity                    19/19 이름·바이트 일치
split part sha256         4/4 OK
재결합 tar sha256         OK  (7,380,474,226 bytes, Ubuntu 원본 해시와 일치)
bundle sha256             OK
git bundle verify         "is okay", "records a complete history"
추출                      exit 0
spot-check                추출 HEAD == metadata HEAD == 03043927c1ea89e8569ab4e9d0bfe0b6d041a47f
```

업로드 중 `HTTP 400 Bad Content-Length` 가 한 번 났다. 원인은 `git_remotes.txt` 가
**0바이트**(원본에 remote 가 없어서). GitHub 가 빈 asset 을 거부한다. 내용을 명시적으로
채워 재시도해 성공. 실패한 release 는 gh 가 롤백해 부분 상태는 남지 않았다.

### paper DGP 식별 — 파일명이 아니라 provenance

```
Stage 1  experiments/decomposition_when_helps/results/run_20260802_112655
Stage 2  reports/temporal_dependence/rho_sweep/pilot_20260803_051713
prereg   3b6b5f89…   data  c889f8f5…   commit@run 03043927…
```

세 독립 식별자(유일한 `factorial_contrasts.csv`, 유일한 C01..C08 8-cell, 보고값
+7.83/−4.58/−6.26/−16.74 정확 재현)에 더해, Windows 쪽에서 미리 잡아둔 네 번째 단서가
맞았다 — 이관 감사가 기록한 `length 576, 384/480/576` 이 prereg 와 정확히 일치.

**이전 내 진술 하나를 정정한다.** `om_factorization_killtest` 를 "paper 것이 아니다" 라고
했는데 너무 셌다. 그 **DGP** 는 paper DGP 가 아니지만(자기 prereg 명시), paper study 가
그 **models 와 trainer 를 그대로 import** 한다.

### 검증된 것

```
marginal 통제   interval: d-1/d+1 교대 vs iid(각 0.5) / magnitude: λ 5,15 교대 vs iid
                양쪽 모두 장기 주변분포 동일, 순서만 다름. audit marginal_control_pass=true
공정성          M0 point 5,856 params == M1 hurdle 5,856. 동일 trainer·optimizer·budget·
                split·seed, checkpoint = validation realized-y MSE, per-cell tuning 금지
metric          rmse_mean_truth (exact DP oracle 대비). gain = 1 - RMSE_h/RMSE_p 은
                세 study 공통이나 delta 부호는 Stage 1 만 반대 -> 표에는 gain 을 쓴다
seed            cell 당 80 계열(40 x data_seed 2), model_seed 2, bootstrap 2000 paired
```

### Stage 1 (8-cell) / Stage 2 (18-cell rho sweep)

```
Stage 1 contrasts (pp)   interval +7.83  magnitude -4.58  sparsity -6.26
                         sparsity×interval +3.35  interval×magnitude -16.74
                         sparsity×magnitude -0.01 (CI 0 포함)
Stage 2 factor           |rho_I| +0.1904 [+0.1699,+0.2119]  vs signed rho_I +0.0667
                         rho_M -0.0711   d×rho_I +0.0332   d×rho_M n.s.
                         분류 CLASS_A_GENERAL_PREDICTABILITY_SUPPORT
Point-우세 cell          18개 중 정확히 1개: d=8, rho_I=0, rho_M=+0.8
                         gain -19.76% [-26.00,-14.53]
```

원본 보고서가 스스로 적어둔 문장: **"This overturns the reading Stage 1 invited."**
Stage 1 의 predictable 은 결정론적 교대(rho=−1) 하나뿐이라 graded 주장 불가.
Stage 1 status 는 `CONDITIONALLY_VALID`.

### H1/H2/H3 provenance

```
H1              EMPIRICAL_ANALOGUE. 외부가 abs(rho_interval) 를 쓰는 게 임의 선택이 아니라
                Stage 2 의 발견 그 자체(|rho_I| 계수가 signed 의 3배).
H2 selector     EMPIRICAL_ANALOGUE, 세 축 1:1 대응(부호까지: 합성 rho_M=+0.8,
                외부 signed 상위 tertile).
H2 mechanism    NOT_REPLICATED. 합성 설계에 scale 축이 아예 없어 외부 confound 에
                답할 수 없다.
H3              CONSTRUCT_MISMATCH. 합성은 ADI 4 vs 8, 외부는 중앙값 1.304/1.317.
                합성 상호작용은 Stage 1·2 양쪽에서 유의.
```

### 판정

```
TRANSPORT_INTEGRITY_PASS
C1-G1~G8 전부 PASS  ->  C1_CONFIRMED
paper_synthesis 대조: MAJOR 0 / MINOR 4 / MATCH 6 / SUPERSEDED 1
Figure 2: BLOCKED -> FIG2_SOURCE_READY
MUST_HAVE: 1 -> 0
```

-> **`PAPER_READY_FOR_OUTLINE`**

routing stop(`ROUTING_MODEL_DEVELOPMENT_STOP`)은 그대로 유지한다.

상태: `SYNTHETIC_SOURCE_WINDOWS_VERIFICATION_READY_FOR_REVIEW`

---

## Verified claim ledger freeze — 표현 6개 고정, 새 발견 2개

새 실험 0회. commit/push 없음. 원본 `results/paper_synthesis/` 미수정.
신규 `results/paper_synthesis_verified/claim_ledger_frozen.md`.

### 제안받은 교정 6개 — 전부 artifact 로 확인 후 수용

```
F1 stage 명칭   Stage 1 = fixed-marginal 2x2x2 / Stage 2 = stationary rho sweep
                mechanism = "Stage 1 component diagnostic". Stage 3/4 폐기.
F2 성능 부호    G = 100(1 - RMSE_H/RMSE_P) 하나로 통일.
                [확인] cell 평균에서 재계산한 main effect 가 저장값과 소수 3자리 일치
                (+7.831 / -4.581 / -6.264). Stage 1 effect_pp 가 이미 이 단위였다.
F3 일반화 근거  Stage 2 > Stage 1. Stage 1 은 CONDITIONALLY_VALID 이고 predictable 이
                결정론적 교대(rho=-1) 하나뿐. 원본 보고서가 "overturns the reading
                Stage 1 invited" 라고 직접 적어둠.
F4 H2 분리      selector transfer 확인 / isolated mechanism 비재현.
F5 mechanism    MECHANISM_AGGREGATE_SUPPORT -> COMPONENT-ATTRIBUTION DIAGNOSTIC SUPPORT.
                hybrid 컬럼은 오차를 귀속시키지 원인을 식별하지 않는다. 하향 수용.
F6 전송 표현    PRIMARY SOURCE PAYLOAD INTEGRITY PASS.
                Ubuntu 가 만든 20개 중 19개 업로드(빠진 1개는 6.9G 원본 tar 자체로,
                조각에서 재결합해 Ubuntu SHA 와 바이트 일치). 원 프로토콜의 inventory
                파일들은 Ubuntu 에서 애초에 생성되지 않았고(붙여넣기 안전판에서 제거),
                동등 분석을 Windows 에서 복원본 대상으로 직접 수행했다.
                "full metadata parity PASS" 라고 쓰지 않는다.
```

### freeze 중 새로 확인한 것 2개 — 둘 다 스토리를 강화

**1. Stage 1 에는 CI 가 0 을 제외하는 Point 우세 cell 이 없다.**
C08(d=8, 양축 independent) 이 -3.01pp 인데 CI [-6.79, +0.23] 로 0 포함.
CI 가 확실한 Point 우세 영역은 Stage 2 에만 있다 (-19.76 [-26.00, -14.53]).
-> **H2 의 합성 출처는 Stage 1 이 아니라 Stage 2 다.**

```
cell  d  interval      magnitude       G(pp)        95% CI
C01   4  alternating   alternating      8.47   [ 6.36, 10.57]
C02   4  alternating   independent     27.81   [25.46, 30.27]
C03   4  independent   alternating     18.76   [17.04, 20.50]
C04   4  independent   independent      8.55   [ 7.14,  9.82]
C05   8  alternating   alternating      3.57   [ 2.18,  5.00]
C06   8  alternating   independent     26.87   [23.78, 30.18]
C07   8  independent   alternating     11.10   [ 8.37, 13.78]
C08   8  independent   independent     -3.01   [-6.79,  0.23]   <- 0 포함
```

**2. 두 축이 비대칭이다.**
occurrence 는 절댓값이 지배 — |rho_I| +0.1904 vs signed +0.0667 (2.9배), 부호 무관.
magnitude 는 부호가 지배 — signed rho_M -0.0711 vs |rho_M| -0.0228 (3.1배), 지속이 Point 편.

이 하나로 두 stage 의 겉보기 모순이 풀린다: Stage 1 의 magnitude 팔은 *교대*라
그 cell 들이 오히려 Hurdle 우세(C03 +18.76, C07 +11.10)였고,
Point 우세는 Stage 2 의 *지속*(+0.8)에서만 나타난다.

### C1 문구 확정

> Controlled characterization of the **finite-sample** relative inductive bias of
> direct and factorized forecasting under temporal occurrence and magnitude dependence.

`finite-sample` 근거: 양측 파라미터 5,856 동일, trainer·30 epoch 예산 공유,
cell 당 80 계열, exact oracle 대비 채점. 측정된 것은 예산 하 추정 거동이지
목표가 같은 두 함수 클래스의 점근적 우열이 아니다.

상태: `PAPER_READY_FOR_OUTLINE`, MUST_HAVE 0, 다음은 detailed outline.

---

## Detailed outline + Figure/Table specification 작성

새 실험 0회. commit/push 없음. `results/paper_synthesis/`·`paper_synthesis_verified/` 미수정(읽기만).
신규 `results/paper_outline_verified/` 6개.

```
narrative_flow.md          한 문단 요약, reader chain, 그림만 읽는 테스트, 구조 변경 제안 2건
detailed_outline.md        섹션별 Q/P/C/E/A/T 6필드
figure_specification.md    Fig 1 / 2A-2C / 3A-3C
table_specification.md     Table 1~4 + appendix 11개
claim_to_evidence_map.md   M1~M20 + 한계 L1~L7, 전부 artifact 연결
reviewer_risk_map.md       R1~R10, 잔여 노출 순위
```

### Fig 2B/2C 용 marginal 을 실제로 계산했다

```
rho_I  -0.8 / 0 / +0.8   mean G  +12.10 / +3.57 / +16.47 pp   -> U자
rho_M  -0.8 / 0 / +0.8   mean G  +14.10 / +11.94 / +6.11 pp   -> 단조 감소
d       4 / 8            mean G  +12.04 / +9.39 pp
```

U자 대 단조 — "occurrence 는 강도, magnitude 는 방향" 이 그림에서 바로 읽힌다.
두 패널은 y축을 공유해야 한다(모양 비교가 목적).

### 구조 제안 2건 (요청 outline 대비)

1. **3장과 4장 병합** -> "Setup". 둘 다 반 페이지이고 앞이 뒤를 동기부여하므로
   따로 두면 질문 없이 끝나는 섹션이 생긴다.
2. **routing 절을 Discussion 앞에** 배치. 5장이 "적응적 선택" 을 함의하므로
   그 루프를 바로 닫아야 부록 같지 않다. 분량은 1페이지 유지.

### Figure 1 결과 누출 차단

Fig 1 에 |rho_I| 중요성·rho_M 부호·우세영역을 넣지 않는다. 세 rho 수준은 **설계 수준**
으로만 등장하고 두 축은 시각적으로 대칭 처리한다. 비대칭은 Fig 2 에서 처음 결과로 제시.

### Fig 2A 색상 주의 (기록해 둠)

17개 cell 이 +2.4~+22.8 인데 문제의 cell 하나가 -19.76 이다. 대칭 diverging scale 을
쓰면 유일하게 중요한 cell 이 중립색으로 보인다. 비대칭 scale 또는 2구간 norm 필요.

상태: `PAPER_OUTLINE_FROZEN_READY_FOR_MANUSCRIPT` (사용자 리뷰 대기, 원고 자동 작성 안 함)

---

## Outline 리뷰 반영 — 구조·그림 사양 최종 freeze

새 실험 0회. commit/push 없음. `paper_synthesis/`·`paper_synthesis_verified/` 미수정.
`results/paper_outline_verified/` 6개 갱신 + `final_outline_freeze.md` 신규.

### 반영한 결정 6건

```
1  3+4장 병합 -> 3장 (3.1 marginal 한계 / 3.2 두 표현 / 3.3 relative finite-sample 비교)
2  routing 독립 6장 폐지 -> 5.7 subsection, 0.5~1페이지. Discussion 은 6장, Conclusion 7장
3  Fig2A 색상: zero-centred SYMMETRIC diverging, [-23,+23], d=4/d=8 동일 scale
4  유의성은 색이 아니라 marker 로 (marker = CI excludes zero)
5  Table 3 = M5/Favorita 만. Fresh/UCI 는 appendix A0 (5.7 에서 한 문장으로 참조)
6  reviewer risk 를 OPEN / MITIGATED / MITIGATED_NOT_ELIMINATED / ACKNOWLEDGED_BOUNDARY 로
   재분류. "공격이 곧 동의" 라는 이전 표현 철회
```

### 내 이전 권고 하나를 정정했다

Fig2A 에 **비대칭 색 scale** 을 권했는데 틀렸다. 내가 걱정한 건 min-max 선형
정규화였고, zero-centred 대칭에서는 양수 17개가 한 hue 계열을 공유하고 음수 cell
하나만 반대 hue 에 단독으로 놓여 오히려 가장 잘 보인다. 비대칭은 불필요한 과장이었다.

### wording 하향

```
"방향이 재현된다"        -> "empirical analogue 가 나타난다"
"정확히 한 곳의 반례"     -> "18-cell grid 안에서 통계적으로 명확한 direct 우세는 한 cell"
"Stage 1 이 Point regime 식별" -> 삭제 (C08 은 -3.01, CI [-6.79,+0.23])
mechanism 무자격 사용     -> synthetic 한정 COMPONENT-ATTRIBUTION DIAGNOSTIC SUPPORT
```

### 검증

FZ1~FZ19 를 실제 파일 내용으로 재검사 -> **FAIL 0**.
(문자열 존재 검사이므로 표현 일관성 확인이지 과학적 검증은 아님 — 그건 앞 단계에서 끝냄.)

상태: `PAPER_STRUCTURE_AND_FIGURE_SPEC_FROZEN`

---

## Figure/Table rendering — spec 확정 + draft 실제 생성

새 실험·학습·TEST 채점 0회. commit/push 없음. frozen 문서·`paper_synthesis/` 미수정.
신규 `results/paper_rendering_verified/` (renderer 2개, spec 4개, drafts 20개, review 2개).

### 렌더는 artifact 에서 직접 읽는다

`render_drafts.py`, `render_tables.py` 는 `synthetic_source_verification/` 과
`external_validity_screen/` 만 읽고, 표시하는 모든 값을 렌더 시점에
`spec/source_map.csv` 에 append 한다 -> 지도와 그림이 어긋날 수 없다.
**mapped 88개, source_file 없는 행 0개, markdown 을 source 로 쓴 행 0개.**
유일한 예외는 Fig1a 의 toy sequence 로, `(schematic)` 으로 명시.

### 렌더하다 발견한 것 (D4/D5 — 새 사실)

**1. `C_neg`/`C_pos` 는 gain 이 아니라 absolute delta 대비다.**
d=4 검산: delta(Im08,Mp00) 0.22672404 − delta(Ip00,Mp00) 0.08599591 = 0.14072813
= 저장된 C_neg 와 **정확히 일치**. 같은 차이를 gain 으로 계산하면 0.08110814 로 불일치.
게다가 rho_M=0 에서만 계산된 값이라 rho_M 에 대한 marginal 도 아니다.
-> G 축에 올리거나 pp 로 병기하면 안 된다. 방향 결론(양쪽 부호 모두 양수)은 영향 없음.
앞서 내 요약 문서들이 이 값을 G 수치 옆에 단위 표기 없이 인용한 것은 표기 결함이고 여기서 정정.

**2. `C_sign` POOLED 의 플래그가 artifact 내부에서 불일치한다.**
value +0.07420, CI [+0.02999, +0.11943] 은 0 을 제외하는데 저장 플래그는 False.
d=4/d=8 행은 자기정합적. 원 run 의 final_report.md 는 CI 를 직접 읽어 "yes, but small".
POOLED 를 AND 규칙으로 본 것 같은데 확정할 수 없으므로, C_sign 유의성은 어느 쪽으로도
인용하지 않고 불일치를 기록한다. 그림·본문 표에 쓰이지 않는 값이다.

### 렌더 결정

```
D1 Fig2a marker  Option B 추천. 18개 중 17개가 유의라 A(유의에 표시)는 17개 dot 로 혼잡.
                 B(비유의 1개만 open circle) + caption 한 줄이 같은 사실을 1개로 전달.
                 A/B 둘 다 렌더해 비교했다. star 는 배제(특별발견처럼 읽힘).
D2 Fig2a 색상    실제 min -19.7643 / max +22.8073 -> |max| 22.81, 대칭 [-23,+23] 확정.
                 **내 이전 "비대칭 scale" 권고는 철회.** 대칭에서 양수 17개가 한 hue,
                 음수 1개만 반대 hue 라 오히려 가장 눈에 띈다 — 렌더로 확인.
D3 Fig2b/c 불확실성  marginal 평균에 대한 bootstrap CI 는 artifact 에 **없다**
                 (primary_contrasts 는 level 간 대비이지 marginal 이 아님).
                 CASE 2 적용 — cell 값을 open circle 로 보이고 "spread, not CI" 명시.
                 cell CI 를 합성해 marginal CI 로 만들지 않았다.
D6 Fig3b/c 단위  첫 draft 가 같은 수치를 (b)-2.30(x100) / (c)-0.023(raw) 로 나란히 표시.
                 둘 다 pp 로 통일.
D7 Fig3b 중복    by_seed.0 과 H2_rule_effect 가 동일값(-0.02304)인데 둘 다 그려
                 독립 추정 4개처럼 보였다. seed 3개 + 3-seed aggregate 로 교체.
D9 폭            venue template 없음 -> 임의 치수 만들지 않고 double(7.16in) 기준 렌더,
                 single(3.5in) 은 venue 확정 후 결정.
```

### QA

V1~V15 전부 PASS, TQ1~TQ10 전부 PASS (렌더된 draft 를 실제로 열어 확인).
reviewer simulation Q1~Q9 중 **Q4 만 PARTIAL** — 그림만으로는 Stage 1 의 구조화 팔이
교대 하나뿐이었다는 게 안 보인다. 렌더 결함이 아니라 caption/본문 의무이며
figure spec 에 caption obligation 으로 박아뒀다.

상태: `PAPER_FIGURES_TABLES_DRAFT_READY_FOR_REVIEW`

---

## Rendering freeze 확정 + Introduction/Contributions 초안

새 실험·학습·TEST 채점 0회. commit/push 없음. frozen 문서·`paper_synthesis/` 미수정.
신규 `results/paper_rendering_verified/final_rendering_freeze.md`,
`results/paper_writing_verified/` 3개.

### 결정 3건 반영

```
1  Fig2A = Option B 확정. artifact 재확인 결과 CI 가 0 을 포함하는 cell 은 정확히
   1개 (d=8, rhoI=0, rhoM=0, G=+2.36 [-1.20,+5.73]).
   최종본 drafts/figure2_final.*, 17-marker 판은
   figure2_comparison_markerA_NOT_FINAL.* 로 비교용만 보존. star 미사용.
2  classical benchmark 는 Table 4 에서 제외 -> Appendix L.
   Table 4 에 classical 행 0개 확인. 절대성능 열세는 5.2 와 Discussion 에서 명시 유지.
3  single-column variant 생성 안 함. Fig 1/2/3 double-column(7.16in) 고정.
   venue 확정 시에만 3.5in 재배치.
```

### warning 3건 freeze

```
UNIT-W1    C_neg/C_pos 는 G(pp) 가 아니라 rho_M=0 slice 의 absolute RMSE-difference
           대비. d=4 검산으로 확정. G 축·pp 표기·Fig2B/C marginal·C1 effect size 로
           사용 금지. 현재 figure/table 미사용.
FLAG-W2    C_sign POOLED 의 CI 는 0 을 제외하는데 저장 플래그는 False.
           원인 추측·재bootstrap 하지 않음. ARTIFACT_INTERNAL_FLAG_INCONSISTENCY,
           NOT USED FOR MANUSCRIPT INFERENTIAL CLAIM 으로 고정.
CAPTION-O1 Stage 1 의 구조화 magnitude 팔이 교대 전용이었다는 사실을
           Stage1->Stage2 전환과 Fig2 caption 에 한 문장으로 명시(그림 복잡화 대신).
```

### Introduction 초안 (6문단)

P1 선택은 관례로 이뤄지고 집계는 사실상 무승부(-0.0007 / -0.0320) -> 질문은 "언제"
P2 ADI/CV2 는 marginal 함수라 순서를 버린다. **기존 correlation 문헌 인정**,
   novelty 는 estimator 가 아니라 *representation* 으로 좁힘
P3 좁힌 질문 + matched budget -> finite-sample inductive bias
P4 Stage 1 상호작용(-16.74 가 주효과보다 큼) -> Stage 2 가 비대칭으로 해소.
   18개 중 direct 우세 1개(-19.8%), 범위 명시
P5 실데이터: analogue + selector 전이(+11.87pp, 3 seed), 그러나 balancing 후
   +0.0032 로 소멸. scale 이 분리 원인이며 합성 설계에 그 축이 없다
P6 oracle 4.11%, 다양성 2.15배, 그런데 -2.43% / -193.9%. stop rule 발동.
   "winner 도 router 도 아니고 characterization + boundary"

### claim audit

SUPPORTED 24 / BOUNDARY_WORDING 11 / CITATION_NEEDED 2 /
**OVERCLAIM 0 / UNSUPPORTED 0**.
IA1~IA15 중 IA9(H3)만 SEE NOTE — 숨긴 게 아니라 Introduction 에 넣지 않았고,
넣으면 caveat(중앙값 분할 vs ADI 4-vs-8)까지 따라와 문단이 무거워진다.
결정은 사용자 몫으로 flag 처리.

숫자 11개 전부 artifact 연결. bibliography 가 repo 에 없어 인용은
[CITATION NEEDED] 2개로만 표시하고 지어내지 않았다.

상태: `INTRODUCTION_AND_CONTRIBUTIONS_DRAFT_READY_FOR_REVIEW`

---

## Introduction v2 — 7문단 재구성, 숫자 11 -> 3, scale 인과 표현 제거

새 실험·학습·TEST 채점 0회. commit/push 없음. v1·frozen 문서 전부 보존.
신규 `results/paper_writing_verified/` 4개 (v2 본문, contributions v2, v2 audit, v1->v2 diff).

### 변경 4건

```
1  P5 분해   v1 의 P5 가 analogue + selector + boundary 세 역할을 겸했다.
             v2 는 P5(transfer) / P6(boundary) 로 나누고 routing·positioning 을 P7 로.
             6문단 -> 7문단. 문단당 96~158 words, 전부 160 미만.
2  H3        Introduction 에 넣지 않기로 확정. 정확히 쓰려면 construct mismatch
             (합성 d=4 vs 8, 외부는 ADI 중앙값 1.304/1.317)까지 붙어야 하고,
             caveat 없이 한 문장만 넣으면 그냥 실패로 읽힌다.
             5.6 / Table 4 / limitations 에 이미 있으므로 은폐 아님. audit IR5 에 사유 기록.
3  숫자      11개 -> 3개. 남긴 것: "eighteen-cell"(범위 없으면 주장 성립 불가),
             "-19.8%"(크기 없이 "direct 우세"는 무의미), "+11.87 pp"(전이 증거 중 최강).
             삭제: -0.0007/-0.0320, -16.74, +0.1064/+0.0789, +0.0032,
             4.11%, 2.15x, -2.43%, +2.648%, -193.9%.
             특히 routing 숫자를 전부 뺐다 — C3 의 숫자 비중이 C1/C2 를 넘으면 안 된다.
4  scale     v1 "the covariate responsible is scale" -> 제거.
             v2 는 "candidate/control 이 scale 에서 실질적으로 다르다" +
             "overlap 보정 후 연관이 0 과 구분되지 않는다" 로, 측정된 것만 진술.
             "confounded" 도 쓰지 않았다 — 이 설계가 감당하지 못하는 인과 약속이 붙는다.
```

### 검사

숫자 3개, 금지어 0, scale 인과 표현 0, C_neg/C_pos·C_sign 미등장,
[CITATION NEEDED] 2개(지어낸 인용 0).
SUPPORTED 20 / BOUNDARY_WORDING 12 / CITATION_NEEDED 2 /
**OVERCLAIM 0 / UNSUPPORTED 0**.
IR1~IR22 전부 PASS. reviewer simulation Q1~Q7 전부 의도대로 읽힘
(v1 에서 PARTIAL 이던 Q4 는 P4 의 "shows that order matters without identifying
which property does" 문장이 해소).

상태: `INTRODUCTION_V2_READY_FOR_REVIEW`

---

## Introduction v3 — P7만 압축, 구조 freeze

새 실험·학습·TEST 채점 0회. commit/push 없음. v1·v2·frozen 문서 전부 보존.
신규 5개: introduction_v3, contributions_v3, v3 audit, v2->v3 diff,
introduction_structure_freeze.

### 변경은 딱 2건

```
P7   158 -> 108 words. routing chronology 전량 삭제
     (target/loss/aggressiveness/capacity/representation 5회 재설계 서술,
      gate-vs-static 수치, expert diversity 확장).
     남긴 것: (A) 하류 질문 (B) complementary error + oracle 이 fixed mixture 상회
     (C) 개발 이득이 domain/time 을 못 넘고 한 외부 domain 이 심각히 악화, stop rule 발동
     (D) universal winner 도 reliable router 도 제안하지 않음, 기여는 characterization+boundary.
     숫자 0개 (digit 자체가 없음).
P1   "the two are close" 절 삭제. aggregate 가 실제로 같지 않은데 동률로 읽힐 여지.
     남은 문장은 "RMSE 와 MAE 가 서로 다른 쪽을 지지한다" 로 실질 유지.
     -> 주장을 **줄이는** 수정이지 늘리는 수정이 아니다.
```

**P2~P6 는 v2 와 바이트 단위 동일**을 문자열 비교로 확인했다 (육안 아님).
contributions 도 헤더 제외 본문이 v2 와 동일.

### 검사에서 걸린 것 2건은 오탐

`tie` -> `quantities`/`properties` 내부 매치.
`reliable router` -> "neither a universal winner nor a generally reliable router" 라는
**부정문** 안. 둘 다 실제 위반 아님을 문맥 출력으로 확인.

### 상태

숫자 3개 유지(18-cell / -19.8% / +11.87 pp, 원본 재확인),
[CITATION NEEDED] 2개 유지, H3 계속 제외(사유 audit IV3-12).
SUPPORTED 20 / BOUNDARY_WORDING 13 / CITATION_NEEDED 2 /
**OVERCLAIM 0 / UNSUPPORTED 0**.
IV3-1~32, IF1~22 전부 PASS. reviewer Q1~Q9 전부 의도대로.

다음 단계는 별도로 "Citation resolution + Related Work boundary audit".
그 전까지 Introduction 은 structure-frozen 이고 P2 만 움직일 예정.

상태: `INTRODUCTION_STRUCTURE_FROZEN_PENDING_CITATIONS`
