# 2026-09-05

## PH-ONLINE-MEMORY-GONO-v1 독립 실험 완료

### 목적과 범위

- 기존 실험과 결과를 수정하지 않고 `experiments/ph_online_memory_gono_v1/`에 독립 프로토콜을 구현했다.
- 산출물은 `results/ph_online_memory_gono_v1/`에만 기록했다.
- M5 29,059개, Favorita 55,561개 적격 시계열을 대상으로 Point/Hurdle 조합의 온라인 메모리 가능성을 failure-first gate로 평가했다.

### Before / After

```text
Before  PH-ONLINE-MEMORY-GONO-v1 전용 구현·사전등록·실행 결과 없음
After   사전등록, Stage 0 재현, 170개 테스트, CUDA smoke, seed0 전체 실행,
        Gate 보고서, Tables A–G, 16절 상태 문서, finalization manifest 완료
```

주요 변경 경로:

- `experiments/ph_online_memory_gono_v1/`: 데이터 계약, 학습, 평가, bootstrap, gate, 재시작, 보고 및 테스트
- `results/ph_online_memory_gono_v1/`: 동결 명세, 실행 provenance, seed0 산출물 및 최종 보고서
- `.gitattributes`: 이 실험의 대형 `.parquet`, `.pt`, 대형 JSON 2개만 Git LFS로 제한

### 사전등록된 최종 판정

```text
Gate0   PASS  oracle opportunity macro 3.911850%
Gate1A  PASS  temporal recurrence, M5 rho 0.223812 / Favorita rho 0.331436
Gate1B  FAIL  B4 vs B3, Favorita -> M5 -0.008979% (< 0.30%)
                    M5 -> Favorita +0.815825%
Final   ONLINE_MEMORY_NO_GO
```

Gate1B는 두 transfer 방향 모두 `RI(B4 vs B3) >= 0.30%`를 요구한다. 한 방향이 실패했으므로 Gate2–4와 조건부 seed1/2는 규칙대로 실행하지 않았다. 이 결과는 retrieval 자체의 무가치나 unsafe를 입증하지 않으며, 단순 B4 온라인 조합이 양방향 기준을 통과하지 못했다는 뜻이다.

### 검증

- 전체 테스트: 170개 PASS
- 전체 프로토콜 및 완료 상태 재호출: exit code 0, 동일 verdict와 동일 artifact SHA
- 동결 구현 71개, 데이터 소스 6개, 환경 binding 일치
- final report, Tables A–G, STATUS, finalization payload/file SHA 일치
- STATUS 번호 섹션 1–16 정확, Tables A–G 행 수 2/2/2/2/1/1/7
- 지정된 기존 결과 디렉터리 13개 모두 baseline과 동일
- seed0 M5/Favorita dataset 및 analysis completion 검증, `failure.json` 0개

커밋 전 대형 결과 1.724 GiB 중 일반 GitHub 한도를 넘는 파일이 확인되어, 해당 실험 경로에만 Git LFS 규칙을 적용했다. 사용자 소유의 다른 미커밋 변경은 이번 커밋에서 제외한다.

## PROB-HEAD-STRUCTURE-FULL-v1 — Task 4 복구 (인계 세션)

Codex 세션이 Task 4 도중 중단되어 테스트 스위트가 깨진 상태를 인계받아 복구했다.
모델 fit은 여전히 0회이며, 과학적 결과는 아직 존재하지 않는다.

### Before / After

```text
Before  test_gates.py 수집 단계 ERROR (모듈 import 불가)
        test_evaluation.py 1건 FAIL (quantile crossing)
        신규 스위트 172개 중 171 pass / 1 fail + 수집 에러 1건
After   신규 스위트 192개 전부 PASS, 저장소 전체 스위트 exit 0 (실패 0, skip 3)
```

### 변경 파일

**1. `tests/prob_head_structure_full_v1/test_evaluation.py`**

`test_validation_selected_p2_applies_to_distinct_sealed_outer_without_cross_split_hash_reuse`의
monkeypatch에 `p_zero` 패치를 추가했다.

- Before: `cdf`만 `1 - exp(-y)`로 패치 → pooled CDF는 `F(0)=0`인데 `p_zero`는 실제 분포값을
  유지해 **유효하지 않은 예측분포**가 생성됨. 12-case 그리드에서 `p_zero >= 0.01`인 case가
  나오면 `q_025`는 zero-mass 규칙으로 0이 되고 `q_010`은 양수라 quantile crossing 발생.
- After: 형제 테스트 `test_p2_uses_bound_native_cdfs_and_frozen_grid_tiebreak`와 동일하게
  `p_zero`를 `torch.zeros_like(self.mu)`로 함께 패치해 `F(0) == p_zero` 불변식 복원.
- 이유: 프로덕션에서 `p_zero = einsum(simplex, teacher_pzero)`이고 pooled CDF의 0 지점 값도
  같은 가중평균이므로 불변식이 구조적으로 성립한다. 즉 구현이 아니라 픽스처가 틀렸고,
  crossing 가드는 정상 동작한 것이다. 구현은 수정하지 않았다.

**2. `experiments/prob_head_structure_full_v1/gates.py`**

`FrozenPrimaryDatasetManifest.from_selection_audit`의 `required_fields`를 producer 계약에 맞췄다.

- Before: 8개 필드 exact-set 검사
- After: `manifest_type`, `selection_manifest_sha256` 추가(10개) +
  `manifest_type == "COUNT_PRIMARY_DATASET_SELECTION_MANIFEST"` 값 검사
- 이유: `data.select_real_datasets`(06:03 수정)가 두 필드를 추가했는데 consumer인
  gates.py(05:55)가 따라가지 못해 import 자체가 실패했다. `manifest_type`은 data.py 1392/2410/2433,
  preregistration.py 1292에서 쓰이는 계통적 무결성 태그이며 `data.verify_real_dataset_selection_manifest`가
  이미 값을 강제한다. gates.py는 "pure reducer"(stdlib+numpy만 import)라 data.py를 import하지 않고
  필드 계약만 맞췄다. `selection_manifest_sha256`은 재계산 검증을 넣지 않았다 — gates.py는 이미
  audits로부터 선택 로직 전체를 재유도하므로 중복이고, 기존 변조 탐지 테스트의 기대 메시지와 충돌한다.

**3. `tests/prob_head_structure_full_v1/test_gates.py`**

`PRIMARY_THREE`를 `select_real_datasets` 경유에서 `from_audit_payload` 직접 구성으로 교체했다.

- Before: `_sealed_count_audit("auto")`, `_sealed_count_audit("carparts")`로 attested
  count-primary audit을 위조해 3-dataset 선택을 생성
- After: gates 계층 정책만 직접 검증 (`selected=("m5","auto","carparts")`)
- 이유: `implementation_plan.md` ruling 12가 "Auto/Carparts/RAF는 6개 evaluation horizon +
  warmup + validation + lookback을 공급할 수 없다"고 동결했고, data.py의
  `CANONICAL_COUNT_PRIMARY_PANEL_CONTRACTS = {m5, online_retail}` + `_verify_sealed_dataset_audit`가
  이를 강제한다. 따라서 3-dataset 선택은 데이터층에서 **구조적으로 도달 불가능**하다.
  gates.py `_validate_policy`는 최대 3개 순서 정책을 정당하게 지원하므로, 동결 계약을 위조하는
  대신 gates 계층에서 그 정책만 검증하도록 했다.

### 검증

```text
tests/prob_head_structure_full_v1   192 passed, exit 0
tests (저장소 전체)                  exit 0, 실패 0, skip 3
gates 함수 커버리지                  s1 s2 s3 r1 r2 r3 a1 a2 a3 a4 b1 b2 c1 c2 c3
                                     negative_control final_recommendation
보호 디렉터리 14개                   git 기준 무변경
```

### 미해결 — 보고 후 사용자 판단 필요

`results/prob_head_structure_full_v1/audit/protected_manifest_before.json`(02:07 동결)은
`schema_version: 1`인데, 현재 `integrity._validate_protected_manifest_structure`는
`schema_version == 2`와 `repository_root_identity` 필드를 요구한다. 그 결과
`verify_protected_manifest_path`가 실제 변조가 없는데도
`EXISTING_ARTIFACT_MUTATION_HARD_STOP`을 던진다(거짓 하드스톱).

원인은 v4 addendum의 "protected baseline을 resolved repository root에 바인딩" 강화로
integrity.py(04:53)가 스키마 v2로 올라간 뒤 02:07 동결 artifact가 갱신되지 않은 것이다.
보호 디렉터리 바이트 자체는 git 기준 무변경이다.

이 파일은 Task 1/7 소유이고 finalization 시 비교 기준이 되므로 임의로 덮어쓰지 않았다.
append-only 규칙상 기존 preregistration v1/v2/v3 + invalidations 패턴과 동일하게
v2 baseline을 새 경로에 추가하고 v1 supersession을 기록하는 방식이 필요하다.

## PROB-HEAD-STRUCTURE-FULL-v1 — 권위 계약 해소 + Task 3 완료 + Task 5 착수

ca717f0을 이어받아 model fit 전에 남아 있던 두 권위 충돌을 해소하고 Task 3을 완성했다.
**모델 fit은 여전히 0회이며 과학적 결과는 아직 존재하지 않는다.**

### 1. preregistration 권위 충돌 해소 (v3 → v4)

충돌 내용: v3는 `numerical_inverse_empirical_CDF`와 256/1024 draw를 scientific reduction으로
읽히게 두었으나, implementation_plan ruling 6은 scientific metric을 deterministic native quantile
또는 linear CDF pool inversion으로 제한하고 empirical sample CDF를 금지한다.

해소 절차 (전부 append-only, 기존 파일 무수정):

```text
preregistration.json          v1  INVALID_BEFORE_ANY_MODEL_FIT  (보존)
preregistered_spec.json       v2  INVALID_BEFORE_ANY_MODEL_FIT  (보존)
preregistered_spec_v3.json    v3  INVALID_BEFORE_ANY_MODEL_FIT  (신규 무효화 기록)
preregistered_spec_v4.json    v4  AUTHORITATIVE_FROZEN_BEFORE_ANY_MODEL_FIT
```

신규 파일: `preregistration_invalidations_v3.json`, `preregistered_spec_v4.json`(+companion,
promotion marker), `preregistration_authority.json`(+companion),
`pre_execution_snapshot_exception.json`(+companion).

**candidate 출처 감사** — 디스크의 04:58 candidate(payload `4c1bdc8b…`)는 현재
`build_preregistered_spec_v4`가 만드는 payload(`473d7c3b…`)와 불일치했다. Codex가 candidate 작성 후
`preregistration.py`를 06:04까지 계속 수정했기 때문이다. 지시문 §2의 "검증 실패 시 재구성" 규칙에 따라
stale candidate를 승격하지 않고, ~20개 v4 단위테스트가 GREEN인 현재 빌더로 재생성해 승격했다.
payload는 세 선행 spec과 모듈 소스의 순수 함수이므로 결과 의존이 원천적으로 불가능하다.

**8개 감사 항목 전부 PASS.** 임계값 보존 여부는 키 이름에 들어간 수치까지 백분율/분수 환산으로 대조했다.
실질 변경 2건은 출처를 명시해 `preregistration_authority.json`에 기록:

- A2/A3/A4 — v3가 A2의 recovery 조건(0.005, CI>0, 2 datasets, 1 dataset CI)을 A3·A4에 중복
  복사했던 것을 프로토콜 §41/§42/§43 위치로 정리. 수치는 전부 보존.
- R2 — `best_identity_operational`을 "10% 행에서 변경"에서 "dataset-level best head 2종 이상"으로
  변경. 프로토콜 §33 항목5 원문의 문자 그대로의 해석이며, v3의 10% 규칙은 프로토콜에 없던 임의
  operationalization이었다. 나머지 임계값(0.15/0.02/1%/0.99) 보존.

### 2. protected manifest schema drift 해소

```text
audit/protected_manifest_before.json      schema v1  historical record (무수정 보존)
audit/protected_manifest_before_v2.json   schema v2  execution comparison baseline
audit/protected_manifest_supersession.json
```

v2는 worktree root(`prob-head-structure-full-v1-worktree`)에 바인딩. 14개 디렉터리 412개 파일.
생성 직후 검증 `status=PASS`, `all_unchanged=True`. 이전에 발생하던 거짓
`EXISTING_ARTIFACT_MUTATION_HARD_STOP`이 해소됐다.

### 3. Task 3 완료 — `experiments/.../training.py` (신규)

v4 training 계약을 그대로 구현: 각 head 자신의 full NLL(분자=valid mask 합, 분모=valid 개수),
전마스킹 행 결정적 제거, validation sCRPS 체크포인트, scheduled epoch (2,4,…,30),
patience 5(strict `score < best`, 동점은 최초 유지), 종료 체크가 terminal,
OOM 1회 재시도(microbatch 1/2·accumulation 2배·effective batch 불변, 2회차는
`OOM_MODEL_BRANCH_BLOCKED`), NaN은 `NUMERICAL_BRANCH_BLOCKED`(learning rate 불변),
append-only attempt + `completion.json` + resume.

### 4. Task 5 착수 (2/5 완료)

- `temporal_features.py` (신규) — origin-bounded 구조 feature 14종 + missing indicator 9종,
  availability-aware train descriptor, fold-median imputer(`ALL_MISSING_TRAIN_FEATURE` 포함).
  경계 밖 값을 바꿔도 feature가 byte-identical함을 테스트로 고정.
- `routing.py` (신규) — regret/soft target(stable softmax), 온도 그리드 (0.25,0.5,1.0),
  k=2 고정 0.25 / k≥3 expanding OOF 선택, expanding cross-fit(fold k는 origin 1..k-1만 사용,
  backfill 금지), class-row expansion + soft sample weight multinomial logistic regression,
  HistGradientBoosting secondary, regret Spearman(상수벡터는 `INSUFFICIENT_VARIATION`).
  **v4에 동결된 inner-origin 리스트를 정확히 재현**: M5 (1493…1689), OnlineRetail (96…122).
  이를 영구 회귀 테스트로 고정했다.

### 검증

```text
신규 실험 테스트    229 passed
저장소 전체         517 passed, exit 0, 실패 0
protected manifest  v2 PASS / all_unchanged True
v4 companion/hash   verify_preregistration OK
authority pointer   v4 해시 일치, 선행 3개 spec 무손상
기존 동결 파일      git 추적 변경 0건
```

### 미완료

- Task 5 잔여: `sensor.py`, `controls.py`, `student.py` 계약 재감사
- Task 6 전체: `run.py`, `reporting.py`, `figures.py`, `__main__.py`, `test_orchestration.py`
- §8 실제 실행 20단계 전부 (Stage S1부터 최종 gate까지) — 아직 시작하지 않음
- 따라서 FINAL RECOMMENDATION 및 A/B/C/HEAD verdict은 산출되지 않았다

## PROB-HEAD-STRUCTURE-FULL-v1 — Task 5 완료

`sensor.py`, `controls.py` 신규 구현으로 Task 5의 5개 파일이 모두 갖춰졌다. 모델 fit은 여전히 0회.

### `sensor.py` (신규)

- disagreement 6종 (`D_zero`/`D_center`/`D_tail`/`D_cdf`/`D_mean`/`D_winner_entropy`) —
  각 horizon step에서 teacher 간 모집단분산(ddof=0) 후 horizon 평균. `D_cdf`는 step별
  target-free support `{0} ∪ 모든 teacher quantile`에서 3쌍 평균 절대 CDF 차.
  `D_winner_entropy`는 temperature 0.5 softmax 엔트로피 (동일 teacher면 log 3).
- delta 4종 + 각각의 missing indicator (직전 origin 없으면 NaN + indicator 1).
- C0 change feature 5종 — prequential 계약대로 `t`에서 결정하고 `[t,t+h)` 실현치까지만 읽는다.
  `t+h` 이후 값을 바꿔도 feature가 불변임을 테스트로 고정.
- geometry — inner pair 규칙 `t>=lookback`이고 `[t,t+2h)`가 model_train 내부.
  **v4 동결 M5 리스트 (1465…1661) 정확히 재현**. OnlineRetail은 유효 pair 0개라
  `REAL_C_SENSOR_GEOMETRY_BLOCKED`. outer feature origin = target origin − h.
- action — 임계값은 validation score의 q80(method='higher'), flag는 strict `>`.
  flagged 행 0개면 `SingleClassMetricUndefined` (0으로 인코딩하지 않음).
  C_A2 widening은 median·p0 보존 후 zero-consistency + cumulative max.
  factor grid (1.05,1.1,1.2,1.35,1.5) 동결.

### `controls.py` (신규)

seed 2026090551 + scope로 SHA256 키잉해 scope만으로 재현 가능한 결정적 control.

- A: teacher identity shuffle(3채널 동시 치환), teacher quantile shuffle(head별 벡터 통째 치환, p0 불변)
- B: regret label shuffle, temporal feature row shuffle (행 전체 보존)
- C: time shuffle(series 내부에서만 origin 치환), teacher name permutation(라벨 대칭 —
  component 최대 절대차 ≤ 1e-12 요구), scale-only, random sensor score(행 키별 U[0,1),
  행 순서와 무관하게 재현)
- 식별 규칙: real effect ≤ 0이면 `IdentificationFailure`, 아니면 recovery = control/real,
  0.5 이상이면 FAIL. 50% 규칙 적용 대상 8종 / 제외 3종을 상수로 고정.

### `student.py` 재감사

v4 계약 부합 확인 — MA25 + Linear(96,h) 2개 + 공유 스칼라 MLP 1→16→22 SiLU,
21점 그리드, `postprocess_student_quantiles`, `quantile_integral_mean`(endpoint-hold).
파라미터 5,838개로 최소 teacher(HSNB 7,098) × 1.5 = 10,647 상한 준수.

### 검증

```text
test_methods.py     55 passed
저장소 전체         545 passed, exit 0, 실패 0
```

### 남은 것

Task 6 전체(`run.py`/`reporting.py`/`figures.py`/`__main__.py`/`test_orchestration.py`)와
지시문 §8 실행 20단계. verdict는 여전히 미산출.
